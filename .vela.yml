version: "1"
steps:
 - name: test
   image: alpine:3.15
   commands:
     - echo "hello"
      
 - name: fake log
   image: golang:1.13
   commands:
     - go install github.com/mingrammer/flog@latest
     - flog -t stdout -f json -b 25000000 -d 1ms
